# Example environment configuration for Imagineer frontend
# Copy this file to .env.development or .env.production and fill in the values

# ============================================================================
# Development Configuration (.env.development)
# ============================================================================
# For local development, use absolute URL to backend:
# VITE_API_BASE_URL=http://localhost:10050/api

# ============================================================================
# Production Configuration (.env.production)
# ============================================================================
# For production deployment on imagineer.joshwentworth.com, use relative path:
# VITE_API_BASE_URL=/api
#
# This works because nginx (frontend) and Flask (backend) are both served
# from the same domain via Cloudflare Tunnel:
# - https://imagineer.joshwentworth.com/* → nginx (React)
# - https://imagineer.joshwentworth.com/api/* → Flask

# API Base URL (change based on environment)
VITE_API_BASE_URL=http://localhost:10050/api

# Password for application access (DEPRECATED - will be replaced by Google OAuth)
# DO NOT commit the actual password - this is just an example
VITE_APP_PASSWORD=your-password-here

# ============================================================================
# Note: Google OAuth is handled entirely by the backend
# The frontend just needs to redirect to /auth/login
# No frontend environment variables required for OAuth!
# ============================================================================
